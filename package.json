{
  "name": "pace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx esbuild assets/app.ts --bundle --minify --outfile=src/app.js && npx tailwindcss -i ./assets/app.css -o ./src/app.css"
  },
  "dependencies": {
    "mithril": "^2.2.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "esbuild": "^0.15.9",
    "@types/mithril": "^2.0.11",
    "tailwindcss": "^3.1.8",
    "typescript": "4.7.2"
  }
}
